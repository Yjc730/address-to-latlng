# address-to-latlng

# 📍 Batch Address to Latitude & Longitude Converter

A simple Python tool to convert multiple addresses into latitude and longitude using OpenStreetMap Nominatim API.

## ✨ Features
- Batch address input
- Automatic latitude / longitude conversion
- Export results to CSV
- Free & open-source (no API key required)

## 🚀 How to Use

### 1. Install dependencies
```bash
pip install -r requirements.txt


# 📍 批次地址轉換經緯度工具（Batch Geocoding Tool）

這是一個使用 Python 撰寫的批次地址轉換工具，
可將多筆地址自動轉換為經緯度（Latitude / Longitude），
並輸出成 CSV 檔案。

## ✨ 功能特色
- 支援一次輸入多筆地址
- 自動轉換為經緯度
- 轉換結果輸出為 CSV
- 使用 OpenStreetMap（免 API Key）

## 🚀 使用方式

### 1️⃣ 安裝套件
```bash
pip install -r requirements.txt
